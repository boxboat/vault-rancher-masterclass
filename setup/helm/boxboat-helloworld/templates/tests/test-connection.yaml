apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boxboat-helloworld.fullname" . }}-test-connection"
  labels:
{{ include "boxboat-helloworld.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "boxboat-helloworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
